import { OnChanges, OnDestroy, SimpleChanges, EventEmitter, ElementRef, Renderer2, ChangeDetectorRef } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Meta } from '@angular/platform-browser';
import { Platform } from '@angular/cdk/platform';
import { Observable } from 'rxjs';
import { ShareService } from './share.service';
import { IShareButton } from './share.models';
export declare class ShareDirective implements OnChanges, OnDestroy {
    private meta;
    private el;
    private http;
    private platform;
    private renderer;
    private cd;
    private shareService;
    private document;
    /** A ref to button class - used to remove previous class when the button type is changed */
    private _buttonClass;
    /** share window closed subscription (to unsubscribe if the button is destroyed before the share window closes) */
    private _shareWindowClosed;
    /** Button properties */
    prop: IShareButton;
    /** Share button type */
    shareButton: string;
    /** Get share count */
    getCount: boolean;
    /** Set meta tags from document head, useful when SEO is supported */
    autoSetMeta: boolean;
    /** Meta tags inputs - initialized from the global options */
    url: string;
    title: string;
    description: string;
    image: string;
    tags: string;
    /** Stream that emits when share count is fetched */
    count: EventEmitter<number>;
    /** Stream that emits when share dialog is opened */
    opened: EventEmitter<string>;
    /** Stream that emits when share dialog is closed */
    closed: EventEmitter<string>;
    constructor(meta: Meta, el: ElementRef, http: HttpClient, platform: Platform, renderer: Renderer2, cd: ChangeDetectorRef, shareService: ShareService, document: any);
    /** Share link on element click */
    onClick(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    /**
     * Open sharing dialog
     * @param url - Share URL
     */
    share(url: string): void;
    shareCount(shareUrl: string): Observable<number>;
    private createShareButton;
    /** Get meta tag content */
    private getMetaTagContent;
    private getPlatform;
}
